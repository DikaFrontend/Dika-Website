window.onload = function() {
    setTimeout(function() {
        document.querySelector('.loading-container').style.display = 'none';  // Hide loading after 3 seconds
    }, 3000);  // Adjust the time here for how long you want the loading to show
};